{
  "name": "shared-contracts_graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ shared-contracts_graph",
    "create-local": "graph create --node http://localhost:8020/ shared-contracts_graph",
    "remove-local": "graph remove --node http://localhost:8020/ shared-contracts_graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 shared-contracts_graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.87.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
